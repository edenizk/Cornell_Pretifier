/**
 * Initializes the newscard module.
 * @constructor
 * @param {Object} el - The newscard element.
 */
function NewsCard (el) {
  this.el = el
  console.warn('Initialized `NewsCard` module.')
}

export default NewsCard
